# Variables de entorno de La Nieta de Portella
# Copia este archivo como ".env" y completa los valores que necesites.
# El archivo ".env" NO se sube al repositorio (esta en .gitignore).

# --- Analytics (opcional) ---
# ID de medicion de Google Analytics 4.
# Si esta vacio, la web NO carga ni envia nada a Google.
# Consiguelo en https://analytics.google.com (formato: G-XXXXXXXXXX)
VITE_GA_ID=""
